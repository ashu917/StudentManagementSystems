<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Student Performance Reports</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(to right, #f9f9f9, #e0f7fa);
    }
    .card {
      border-radius: 15px;
    }
    .card-header {
      background-color: #0d6efd;
      color: white;
      font-weight: bold;
    }
    .card-body label {
      font-weight: 600;
      margin-top: 10px;
    }
    .card + .card {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container py-4">
    <h2 class="text-center text-primary mb-4">📊 Student Performance Reports</h2>

    <% if (performances.length === 0) { %>
      <div class="alert alert-warning text-center">
        No performance data available yet.
      </div>
    <% } else { %>
      <div class="row">
        <% performances.forEach(perf => { %>
          <div class="col-md-6">
            <div class="card shadow-sm">
              <div class="card-header">
                <%= perf.name %> (<%= perf.email %>)
              </div>
              <div class="card-body">
                <p><strong>Attendance:</strong> <%= perf.attendance %> %</p>
                <p><strong>Assignments Done:</strong> <%= perf.assignments_done %></p>
                <p><strong>Projects:</strong> <%= perf.projects || 'N/A' %></p>
                <p><strong>Participation:</strong> <%= perf.participation || 'N/A' %></p>
                <p><strong>Behavior Rating:</strong> <span class="badge bg-info text-dark"><%= perf.behavior_rating %></span></p>
                <p><strong>Teacher Remarks:</strong> <%= perf.teacher_remarks || 'N/A' %></p>
                <p class="text-end text-muted small">Last Updated: <%= new Date(perf.updated_at).toLocaleDateString() %></p>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>

    <div class="text-center mt-4">
      <a href="/admin/dashboard" class="btn btn-secondary">🔙 Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
