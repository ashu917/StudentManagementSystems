<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
      min-height: 100vh;
    }
    .profile-card {
      background: #ffffffdd;
      border-radius: 15px;
      box-shadow: 0 4px 25px rgba(0,0,0,0.1);
      padding: 30px;
    }
    .profile-title {
      font-size: 24px;
      font-weight: 700;
      color: #2c3e50;
    }
    .profile-label {
      font-weight: 600;
      color: #555;
    }
    .edit-btn {
      width: 150px;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="profile-card">
          <div class="text-center mb-4">
            <h2 class="profile-title">👤 My Profile</h2>
            <p class="text-muted">Welcome, <%= student.name %>!</p>
          </div>

          <div class="row g-3">
            <div class="col-md-6">
              <label class="profile-label">Full Name</label>
              <div class="form-control"><%= student.name %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Email</label>
              <div class="form-control"><%= student.email %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Course</label>
              <div class="form-control"><%= student.course %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Fees</label>
              <div class="form-control">₹ <%= student.fees.toLocaleString('en-IN') %></div>
            </div>
            <div class="col-md-4">
              <label class="profile-label">Age</label>
              <div class="form-control"><%= student.age %></div>
            </div>
            <div class="col-md-4">
              <label class="profile-label">Gender</label>
              <div class="form-control"><%= student.gender %></div>
            </div>
            <div class="col-md-4">
  <label class="profile-label">Date of Birth</label>
  <div class="form-control"><%= student.dob ? student.dob.toDateString() : 'N/A' %></div>
</div>

            </div>
            <div class="col-12">
              <label class="profile-label">Address</label>
              <div class="form-control"><%= student.address %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Phone</label>
              <div class="form-control"><%= student.phone %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Admission Date</label>
              <div class="form-control"><%= student.admission_date.toDateString() %></div>
            </div>
            <div class="col-md-6">
              <label class="profile-label">Status</label>
              <div class="form-control"><%= student.status %></div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
